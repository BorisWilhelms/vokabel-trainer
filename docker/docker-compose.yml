services:
  vokabeltrainer:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - vokabeltrainer-data:/app/data
    restart: unless-stopped

volumes:
  vokabeltrainer-data:
